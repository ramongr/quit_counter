{
  "name": "quit_counter",
  "version": "1.0.0",
  "private": true,
  "description": "Time since Lúcia should've quit her job",
  "scripts": {
    "lint": "npm run lint:html && npm run lint:js",
    "lint:html": "html-validate index.html",
    "lint:js": "oxlint",
    "lint:fix": "oxlint --fix"
  },
  "devDependencies": {
    "html-validate": "^11.5.6",
    "oxlint": "^1.73.0"
  }
}
